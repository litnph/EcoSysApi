namespace PFP.Infrastructure.Identity;

/// <summary>Custom JWT claim names issued by <see cref="JwtTokenService"/>.</summary>
internal static class JwtClaimNames
{
    public const string SessionId = "session_id";
}
